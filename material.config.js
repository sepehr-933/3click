import {createTheme} from '@mui/material/styles'

export const theme = createTheme({
    palette: {
                primary: {
                    main: 'rgb(250, 107, 107)',
                    light: '#ff7372',
                    dark : '#fc4174',
                },
                secondary: {
                   main: '#fff'
                },
    },
});